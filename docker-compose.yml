services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: smart-home-ai-dev:latest
    container_name: smart-home-ai-dev
    working_dir: /workspace/smart_home_AI
    volumes:
      - .:/workspace/smart_home_AI
    tty: true
    stdin_open: true
    command: /bin/bash
