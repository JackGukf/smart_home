# Tests

Use this folder for unit tests and integration tests.

Recommended split:

- Unit tests for parsing config and rule logic.
- Mocked tests for vendor adapters.
- Integration tests that only run when real devices are available.
